import styled, { css } from 'styled-components';

export const Container = styled.div`
  width: 100%;
`;

export const PostEventTitle = styled.div`
  padding: 0px 16px 16px;
  font-size: 12px;
  font-weight: normal;
  color: var(--gray-100);
`;

export const PostEventTitleDate = styled.div`
  display: flex;
  margin: 4px 0 4px 0;
  gap: 4px;
  align-items: center;
`;

export const PostEventTitleAddress = styled.div`
  display: flex;
  margin: 4px 0 4px 0;
  gap: 4px;
  align-items: center;
`;

export const PostEventImg = styled.img`
  width: 100%;
`;
