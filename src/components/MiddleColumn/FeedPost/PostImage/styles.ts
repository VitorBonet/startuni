import styled, { css } from 'styled-components';

export const Container = styled.div`
  width: 100%;
`;

export const PostImageTitle = styled.div`
  padding: 0px 16px 16px;
  font-size: 12px;
  font-weight: normal;
      color: var(--gray-700);
`;

export const PostImageImg = styled.img`
  width: 100%;
`;
